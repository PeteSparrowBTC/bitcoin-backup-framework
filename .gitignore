# Generated by scripts/build-site.sh from README.md and START-HERE.md.
# Committing it would mean two copies of the same prose, which is the failure
# the one-guide plan exists to prevent.
/content/

# Hugo build output and caches.
/public/
/resources/
.hugo_build.lock

# Created by "hugo mod vendor"; the module is fetched at build time instead.
/_vendor/
